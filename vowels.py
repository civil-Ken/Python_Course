#check = input("Does it have the all the vowels? ")
##if check == ("a") or ("e") or ("i") or ("o") or ("u"):
 #   print ("YES")
#else:
 #   print ("NO")

check = input ("Does it have all the vowels? ")
vowels= ("a","e","i","o","u")
for i in [check]:
    print(i)
    if i == ("a") or ("e") or ("i") or ("o") or ("u"):
        print ("YES")
    else:
        print ("NO")
